from __future__ import annotations

import configparser
from argparse import Namespace


def build(args: Namespace) -> int:
    '''makes schema installable'''

    config = configparser.ConfigParser()
    config['']

    return 0
